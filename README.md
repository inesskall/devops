# Event Booking API || TicketHub

REST API для системы бронирования событий, разработанная на Spring Boot. Приложение позволяет управлять событиями (конференции, мастер-классы, фестивали) и бронированиями на них.

## 📋 Содержание

- [Технологии](#технологии)
- [Функциональность](#функциональность)
- [Требования](#требования)
- [Установка и запуск](#установка-и-запуск)
- [API Endpoints](#api-endpoints)
- [Telegram Bot](#telegram-bot)
- [Структура проекта](#структура-проекта)
- [База данных](#база-данных)
- [Frontend](#frontend)
- [Тестирование](#тестирование)

## 🛠 Технологии

### Backend:
- **Java 21**
- **Spring Boot 3.4.1**
- **Spring Data JPA**
- **PostgreSQL 16**
- **Hibernate**
- **Lombok**
- **Maven**
- **Swagger/OpenAPI 3**
- **Docker & Docker Compose**

### Telegram Bot:
- **Python 3.8+**
- **python-telegram-bot 20.7**
- **requests 2.31.0**
- **python-dotenv 1.0.0**

## ✨ Функциональность

### События (Events)
- ✅ Создание, чтение, обновление и удаление событий
- ✅ Поиск событий по датам доступности
- ✅ Пагинация и сортировка списка событий
- ✅ Фильтрация по типу события (CONFERENCE, WORKSHOP, FESTIVAL, CONCERT)
- ✅ Управление статусом события (активно/неактивно)
- ✅ Автоматическая генерация ID с корректной последовательностью

### Бронирования (Reservations)
- ✅ Создание бронирования на событие
- ✅ Удаление бронирования
- ✅ Проверка лимита мест (максимум 100 мест на событие)
- ✅ **Защита от двойной регистрации**: один ID студента не может зарегистрироваться дважды на одно событие
- ✅ Отслеживание количества забронированных мест (например, 5/100)
- ✅ Проверка активности события перед бронированием

### Frontend
- ✅ Веб-интерфейс для просмотра событий
- ✅ Фильтрация событий по категориям
- ✅ Поиск событий по названию
- ✅ Форма бронирования с валидацией
- ✅ Отображение количества свободных мест
- ✅ Адаптивный дизайн

### Telegram Bot 🤖
- ✅ Telegram бот для просмотра и бронирования событий
- ✅ Регистрация и авторизация пользователей через бота
- ✅ Просмотр списка всех событий
- ✅ Просмотр деталей конкретного события
- ✅ Бронирование событий через бота
- ✅ Просмотр своих бронирований
- ✅ Интерактивные кнопки для навигации
- ✅ Меню команд в Telegram

## 📦 Требования

### Для Spring Boot приложения:
- Java 21 или выше
- Maven 3.6+
- PostgreSQL 16 (или Docker для запуска через docker-compose)
- Git

### Для Telegram бота (опционально):
- Python 3.8 или выше
- pip (менеджер пакетов Python)

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd EventBookingSpringBootBackEnd
```

### 2. Запуск PostgreSQL через Docker Compose

```bash
docker-compose up -d
```

Это создаст контейнер PostgreSQL с настройками:
- **База данных**: `event_reservation_db`
- **Пользователь**: `event_user`
- **Пароль**: `event_pass`
- **Порт**: `5432`

### 3. Настройка базы данных

Если вы используете существующую базу данных PostgreSQL, выполните SQL скрипты для создания enum типа:

```sql
-- Создание enum типа для типов событий
CREATE TYPE valid_types_of_hotels_enum AS ENUM ('CONFERENCE', 'WORKSHOP', 'FESTIVAL', 'CONCERT');
```

Скрипт находится в файле `create_enum_type.sql`.

### 4. Запуск приложения

#### Через Maven:

```bash
./mvnw spring-boot:run
```

#### Или через скомпилированный JAR:

```bash
./mvnw clean package
java -jar target/event-booking-api-0.0.1-SNAPSHOT.jar
```
#### Или через minikube:
```bash
minikube start
kubectl apply -f k8s/postgres-pvc.yaml
kubectl apply -f k8s/postgres-deployment.yaml
kubectl apply -f k8s/postgres-service.yaml
kubectl apply -f k8s/event-reservation-deployment.yaml
kubectl apply -f k8s/event-reservation-service.yaml

kubectl get pods
kubectl get svc

minikube service event-reservation-api --url
```
Приложение запустится на порту **8080**.

### 5. Доступ к приложению

- **API**: http://localhost:8080/api/v1
- **Frontend**: http://localhost:8080/index.html
- **Swagger UI**: http://localhost:8080/swagger-ui/index.html

### 6. Запуск Telegram бота (опционально)

Подробные инструкции по установке и запуску Telegram бота находятся в [telegram_bot/README.md](telegram_bot/README.md).

**Быстрый старт:**

```bash
cd telegram_bot

# Установка зависимостей
pip3 install -r requirements.txt

# Настройка токена (создайте .env файл из .env.example)
cp .env.example .env
# Отредактируйте .env и укажите TELEGRAM_BOT_TOKEN

# Запуск бота
./start_bot.sh
```

**Требования для бота:**
- Python 3.8+
- Токен Telegram бота от [@BotFather](https://t.me/BotFather)
- Запущенное Spring Boot приложение

## 📡 API Endpoints

### События (Events)

#### Получить все события
```http
GET /api/v1/events
```

#### Получить событие по ID
```http
GET /api/v1/event/{id}
```

#### Получить события с пагинацией
```http
GET /api/v1/eventPagedList?pageNumber=0&pageSize=10&sortBy=id
```

#### Поиск событий по датам
```http
GET /api/v1/events/availabilitySearch?dateFrom=2025-11-01&dateTo=2025-11-30
```

#### Создать событие
```http
POST /api/v1/event
Content-Type: application/json

{
  "name": "Конференция по ИИ",
  "type": "CONFERENCE",
  "description": "Узнайте о последних достижениях в области искусственного интеллекта!",
  "availableFrom": "2025-11-28",
  "availableTo": "2025-11-29",
  "status": true
}
```

**Типы событий**: `CONFERENCE`, `WORKSHOP`, `FESTIVAL`, `CONCERT`

#### Обновить событие
```http
PATCH /api/v1/event
Content-Type: application/json

{
  "id": 1,
  "name": "Обновленное название",
  "type": "WORKSHOP",
  "description": "Новое описание",
  "availableFrom": "2025-12-01",
  "availableTo": "2025-12-02",
  "status": true
}
```

#### Удалить событие
```http
DELETE /api/v1/event/{id}
```

### Бронирования (Reservations)

#### Получить все бронирования
```http
GET /api/v1/reservations
```

#### Получить бронирование по ID
```http
GET /api/v1/reservation/{id}
```

#### Создать бронирование
```http
POST /api/v1/reservation
Content-Type: application/json

{
  "eventId": 1,
  "checkIn": "STUDENT123",
  "status": true
}
```

**Важно**: 
- Поле `checkIn` используется для хранения ID студента
- Один ID студента не может зарегистрироваться дважды на одно событие
- Максимум 100 бронирований на одно событие
- Событие должно быть активным (`status: true`)

#### Удалить бронирование
```http
DELETE /api/v1/reservation/{id}
```

### Пользователи (Users)

#### Регистрация нового пользователя
```http
POST /api/v1/register
Content-Type: application/json

{
  "studentId": "STUDENT123",
  "name": "Иван",
  "surname": "Иванов",
  "password": "password123"
}
```

#### Вход в систему
```http
POST /api/v1/login
Content-Type: application/json

{
  "studentId": "STUDENT123",
  "password": "password123"
}
```

#### Получить текущего пользователя
```http
GET /api/v1/user/current
```

#### Выход из системы
```http
POST /api/v1/logout
```

## 🤖 Telegram Bot

Проект включает Telegram бота для удобного доступа к системе бронирования через мессенджер.

### Возможности бота:

- 📝 **Регистрация и авторизация** - создание аккаунта и вход через бота
- 📅 **Просмотр событий** - список всех доступных событий с деталями
- 🎫 **Бронирование** - быстрое бронирование событий через интерактивные кнопки
- 📋 **Мои бронирования** - просмотр всех ваших бронирований
- 🎯 **Интерактивный интерфейс** - удобные кнопки и меню команд

### Установка и запуск бота:

Подробные инструкции находятся в [telegram_bot/README.md](telegram_bot/README.md)

**Краткая инструкция:**

1. Перейдите в директорию бота:
   ```bash
   cd telegram_bot
   ```

2. Установите зависимости:
   ```bash
   pip3 install -r requirements.txt
   ```

3. Создайте бота в Telegram через [@BotFather](https://t.me/BotFather)

4. Настройте `.env` файл:
   ```bash
   cp .env.example .env
   # Отредактируйте .env и укажите TELEGRAM_BOT_TOKEN
   ```

5. Запустите бота:
   ```bash
   ./start_bot.sh
   ```

### Команды бота:

- `/start` - Начать работу с ботом
- `/register` - Регистрация нового аккаунта
- `/login` - Вход в существующий аккаунт
- `/events` - Показать все события
- `/event <id>` - Показать детали события
- `/book` - Забронировать событие
- `/my_reservations` - Мои бронирования
- `/logout` - Выйти из аккаунта
- `/help` - Справка

## 📁 Структура проекта

```
EventBookingSpringBootBackEnd/
├── src/
│   ├── main/
│   │   ├── java/dev/yerassyl/aliyev/
│   │   │   ├── bootstrap/          # Инициализация тестовых данных
│   │   │   ├── config/             # Конфигурация (CORS, OpenAPI)
│   │   │   ├── constants/          # Константы приложения
│   │   │   ├── controller/         # REST контроллеры
│   │   │   ├── dto/                # Data Transfer Objects
│   │   │   ├── entity/             # JPA сущности
│   │   │   ├── exception/          # Обработка исключений
│   │   │   ├── repository/         # JPA репозитории
│   │   │   ├── service/             # Интерфейсы сервисов
│   │   │   ├── serviceImp/         # Реализация сервисов
│   │   │   ├── utils/              # Утилиты
│   │   │   └── validator/          # Валидаторы
│   │   └── resources/
│   │       ├── application.properties    # Основные настройки
│   │       ├── application-h2.properties # H2 профиль
│   │       ├── application-mysql.properties # MySQL профиль
│   │       └── static/              # Frontend файлы
│   │           ├── index.html
│   │           ├── login.html
│   │           ├── register.html
│   │           ├── script.js
│   │           ├── styles.css
│   │           └── img/             # Изображения событий
│   └── test/                        # Тесты
├── telegram_bot/                    # Telegram бот
│   ├── bot.py                      # Основной файл бота
│   ├── api_client.py               # Клиент для работы с API
│   ├── config.py                   # Конфигурация
│   ├── requirements.txt            # Зависимости Python
│   ├── start_bot.sh                # Скрипт запуска
│   ├── stop_bot.sh                 # Скрипт остановки
│   ├── .env.example                # Пример настроек
│   └── README.md                   # Документация бота
├── docker-compose.yml              # Docker Compose конфигурация
├── pom.xml                          # Maven зависимости
└── README.md                        # Документация
```

## 🗄 База данных

### Схема базы данных

#### Таблица `event`
- `id` (SERIAL PRIMARY KEY) - Автоматически генерируемый ID
- `name` (VARCHAR) - Название события
- `type` (valid_types_of_hotels_enum) - Тип события
- `description` (TEXT) - Описание события
- `available_from` (DATE) - Дата начала доступности
- `available_to` (DATE) - Дата окончания доступности
- `status` (BOOLEAN) - Статус активности события
- `created_date` (TIMESTAMP) - Дата создания (из AuditableEntity)
- `last_modified_date` (TIMESTAMP) - Дата последнего изменения (из AuditableEntity)

#### Таблица `reservation`
- `id` (SERIAL PRIMARY KEY) - Автоматически генерируемый ID
- `event_id` (INTEGER NOT NULL) - ID события
- `check_in` (VARCHAR) - ID студента (nullable)
- `status` (BOOLEAN) - Статус бронирования
- `created_date` (TIMESTAMP) - Дата создания
- `last_modified_date` (TIMESTAMP) - Дата последнего изменения

#### Таблица `user_table`
- `id` (SERIAL PRIMARY KEY) - Автоматически генерируемый ID
- `student_id` (VARCHAR, UNIQUE, NOT NULL) - Уникальный ID студента
- `name` (VARCHAR, NOT NULL) - Имя пользователя
- `surname` (VARCHAR, NOT NULL) - Фамилия пользователя
- `password` (VARCHAR, NOT NULL) - Пароль (хэшированный)
- `created_date` (TIMESTAMP) - Дата создания
- `last_modified_date` (TIMESTAMP) - Дата последнего изменения

### SQL скрипты

В проекте есть несколько SQL скриптов для настройки базы данных:

- `create_enum_type.sql` - Создание enum типа для типов событий
- `fix_id_columns.sql` - Исправление ID колонок
- `remove_checkin_checkout_guests.sql` - Удаление NOT NULL ограничений

## 🎨 Frontend

Frontend приложение находится в `src/main/resources/static/` и включает:

- **index.html** - Главная страница с интерфейсом бронирования
- **script.js** - JavaScript логика для работы с API
- **styles.css** - Стили приложения
- **img/** - Изображения событий (event-1-img.jpg, event-2-img.jpg, и т.д.)

### Функции Frontend:

- Отображение списка событий с фильтрацией
- Поиск событий по названию
- Фильтрация по категориям (Конференция, Мастер-класс, Фестиваль)
- Форма бронирования с валидацией
- Отображение количества забронированных мест (например, 5/100)
- Модальное окно для бронирования

## 🧪 Тестирование

Запуск тестов:

```bash
./mvnw test
```

Тесты включают:
- Unit тесты для валидаторов
- Mockito тесты для сервисов
- Интеграционные тесты

## ⚙️ Конфигурация

### application.properties

Основные настройки находятся в `src/main/resources/application.properties`:

```properties
# Порт приложения
server.port=8080

# База данных PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/event_reservation_db
spring.datasource.username=event_user
spring.datasource.password=event_pass

# Hibernate настройки
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
```

### CORS

Приложение настроено для работы с фронтендом. CORS конфигурация находится в `CorsConfig.java`.

## 🔒 Безопасность и валидация

- Валидация входных данных через Bean Validation
- Проверка существования сущностей перед операциями
- Защита от двойной регистрации на одно событие
- Проверка лимита мест (максимум 100)
- Проверка активности события перед бронированием

## 📝 Логирование

Логи сохраняются в файл `event-reservation-api.log` в корне проекта. Уровень логирования для пакета `dev.yerassyl.aliyev` установлен на `DEBUG`.

## 🐛 Известные проблемы и решения

### Проблема с последовательностью ID после удаления

После удаления событий новые события могут получать неправильные ID. Это решено автоматическим обновлением последовательности PostgreSQL после каждого удаления.

### Двойная регистрация

Система предотвращает двойную регистрацию одного ID студента на одно событие, используя поле `checkIn` для хранения ID студента.

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👤 Автор

Разработано для системы бронирования событий.

## 🤝 Вклад

Если вы хотите внести вклад в проект, пожалуйста, создайте issue или pull request.

---

**Примечание**: Убедитесь, что PostgreSQL запущен и доступен перед запуском приложения. Используйте `docker-compose up -d` для быстрого запуска базы данных.
