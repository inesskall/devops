# Telegram Bot для бронирования событий

Telegram бот для просмотра и бронирования событий через API Event Booking.

## 📋 Требования

- **Python 3.8 или выше**
- **Запущенное Spring Boot приложение** (доступное по адресу, указанному в `API_BASE_URL`)
- **Токен Telegram бота** от [@BotFather](https://t.me/BotFather)
- **Интернет-соединение** для работы с Telegram API и вашим Spring Boot API

## 🚀 Установка и настройка

### Шаг 1: Проверка Python

Убедитесь, что Python установлен:

```bash
python3 --version
```

Если Python не установлен:

**macOS:**
```bash
# Используйте Homebrew
brew install python3
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install python3 python3-pip
```

**Windows:**
Скачайте и установите Python с [python.org](https://www.python.org/downloads/)

### Шаг 2: Клонирование/копирование проекта

Если вы используете git:
```bash
git clone <repository-url>
cd EventBookingSpringBootBackEnd/telegram_bot
```

Или просто скопируйте папку `telegram_bot` на новое устройство.

### Шаг 3: Установка зависимостей

Перейдите в директорию бота:
```bash
cd telegram_bot
```

Установите зависимости Python:

**Рекомендуется использовать виртуальное окружение:**

**macOS/Linux:**
```bash
# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

**Windows:**
```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

**Или установите напрямую (без виртуального окружения):**
```bash
pip3 install -r requirements.txt
```

**Зависимости:**
- `python-telegram-bot==20.7` - библиотека для работы с Telegram Bot API
- `requests==2.31.0` - для HTTP запросов к вашему API
- `python-dotenv==1.0.0` - для работы с переменными окружения

### Шаг 4: Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Event Booking Bot")
   - Введите username бота (должен заканчиваться на `bot`, например: `event_booking_bot`)
4. Сохраните полученный токен (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 5: Настройка переменных окружения

1. Скопируйте файл примера:
```bash
cp .env.example .env
```

2. Откройте файл `.env` в текстовом редакторе:
```bash
# macOS/Linux
nano .env
# или
vim .env

# Windows
notepad .env
```

3. Заполните переменные:
```env
# Telegram Bot Token (получить у @BotFather в Telegram)
TELEGRAM_BOT_TOKEN=ваш_токен_здесь

# API базовый URL (по умолчанию localhost:8080)
# Если API на другом сервере, укажите полный URL
API_BASE_URL=http://localhost:8080/api/v1
```

**Пример заполненного `.env` файла:**
```env
TELEGRAM_BOT_TOKEN=8385165399:AAE4fJgI-axqA4SjaTePuZ9Ma_SjnjeKJjU
API_BASE_URL=http://localhost:8080/api/v1
```

**Если API на удаленном сервере:**
```env
TELEGRAM_BOT_TOKEN=ваш_токен
API_BASE_URL=http://your-server.com:8080/api/v1
```

### Шаг 6: Проверка Spring Boot приложения

Убедитесь, что ваше Spring Boot приложение запущено и доступно:

```bash
# Проверьте, что API отвечает
curl http://localhost:8080/api/v1/events
```

Если API на другом адресе, замените URL соответственно.

## ▶️ Запуск бота

### Способ 1: Использование скрипта (рекомендуется)

**macOS/Linux:**
```bash
cd telegram_bot
chmod +x start_bot.sh  # Только при первом запуске
./start_bot.sh
```

**Windows:**
```bash
cd telegram_bot
python bot.py
```

### Способ 2: Прямой запуск

```bash
cd telegram_bot

# Если используете виртуальное окружение, активируйте его сначала
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate  # Windows

python3 bot.py
```

### Способ 3: Запуск в фоновом режиме (Linux/macOS)

```bash
cd telegram_bot
nohup python3 bot.py > bot.log 2>&1 &
```

Логи будут сохраняться в файл `bot.log`.

## 🛑 Остановка бота

### Способ 1: Использование скрипта

```bash
cd telegram_bot
./stop_bot.sh
```

### Способ 2: Через команду

```bash
pkill -f "python3.*bot.py"
```

### Способ 3: Если бот запущен в текущем терминале

Нажмите `Ctrl+C`

### Проверка статуса бота

```bash
pgrep -f "python3.*bot.py" && echo "Бот запущен" || echo "Бот не запущен"
```

## 📱 Использование бота

### Первый запуск

1. Найдите вашего бота в Telegram (по username, который вы указали при создании)
2. Отправьте команду `/start`
3. Вы увидите приветственное сообщение с кнопками "📝 Регистрация" и "🔐 Вход"

### Регистрация нового пользователя

1. Нажмите кнопку "📝 Регистрация" или отправьте команду `/register`
2. Следуйте инструкциям:
   - Введите ваше имя
   - Введите вашу фамилию
   - Введите Student ID
   - Введите пароль (минимум 4 символа)

### Вход в существующий аккаунт

1. Нажмите кнопку "🔐 Вход" или отправьте команду `/login`
2. Введите ваш Student ID
3. Введите ваш пароль

### Доступные команды

**Основные команды:**
- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/register` - Регистрация нового аккаунта
- `/login` - Вход в существующий аккаунт
- `/logout` - Выйти из аккаунта

**Команды для работы с событиями (требуют авторизации):**
- `/events` - Показать список всех событий
- `/event <id>` - Показать детали конкретного события (например: `/event 1`)
- `/book` - Начать процесс бронирования события
- `/my_reservations` - Показать мои бронирования
- `/cancel` - Отменить текущую операцию

### Меню команд

В Telegram бот имеет меню команд (кнопка рядом со стикером), где доступны все основные команды для быстрого доступа.

### Процесс бронирования

1. Убедитесь, что вы авторизованы (`/login` или `/register`)
2. Используйте команду `/events` для просмотра доступных событий
3. Выберите событие из списка или используйте `/event <id>` для просмотра деталей
4. Нажмите кнопку "📝 Забронировать" в деталях события или используйте команду `/book`
5. Если использовали `/book`, введите ID события
6. Если пароль не сохранен, введите пароль для подтверждения
7. Бронирование будет создано автоматически

## 📁 Структура проекта

```
telegram_bot/
├── bot.py              # Основной файл бота с обработчиками команд
├── api_client.py       # Клиент для работы с REST API
├── config.py           # Конфигурация (загрузка переменных окружения)
├── requirements.txt    # Зависимости Python
├── start_bot.sh        # Скрипт для запуска бота (macOS/Linux)
├── stop_bot.sh         # Скрипт для остановки бота (macOS/Linux)
├── .env.example        # Пример файла с переменными окружения
├── .env                # Файл с реальными переменными (создается вручную)
├── .gitignore          # Игнорируемые файлы для git
└── README.md           # Этот файл
```

## 🔧 Решение проблем

### Бот не запускается

**Ошибка: "TELEGRAM_BOT_TOKEN не установлен"**
- Убедитесь, что файл `.env` существует
- Проверьте, что токен указан правильно в файле `.env`
- Убедитесь, что нет лишних пробелов в файле `.env`

**Ошибка: "ModuleNotFoundError"**
- Установите зависимости: `pip3 install -r requirements.txt`
- Если используете виртуальное окружение, убедитесь, что оно активировано

**Ошибка: "Connection refused" при работе с API**
- Проверьте, что Spring Boot приложение запущено
- Проверьте адрес в `API_BASE_URL` в файле `.env`
- Убедитесь, что API доступен: `curl http://localhost:8080/api/v1/events`

### Бот не отвечает на команды

- Проверьте логи бота на наличие ошибок
- Убедитесь, что токен правильный
- Проверьте интернет-соединение
- Убедитесь, что бот запущен (проверьте процесс)

### Проблемы с авторизацией

- Убедитесь, что пользователь существует в базе данных Spring Boot приложения
- Проверьте правильность Student ID и пароля
- Убедитесь, что API доступен и работает корректно

### Просмотр логов

Если бот запущен в фоновом режиме:
```bash
tail -f telegram_bot/bot.log
```

Или проверьте вывод в терминале, если бот запущен напрямую.

## 🔐 Безопасность

- **НЕ коммитьте** файл `.env` в git (он уже в `.gitignore`)
- **НЕ делитесь** токеном бота публично
- Храните `.env` файл в безопасном месте
- Используйте сильные пароли для пользователей

## 📝 Дополнительная информация

### Изменение адреса API

Если ваше Spring Boot приложение работает на другом адресе или порту, измените `API_BASE_URL` в файле `.env`:

```env
API_BASE_URL=http://your-server.com:8080/api/v1
```

### Обновление зависимостей

```bash
pip3 install --upgrade -r requirements.txt
```

### Перезапуск бота после изменений

1. Остановите бота: `./stop_bot.sh`
2. Внесите изменения в код
3. Запустите бота снова: `./start_bot.sh`

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки в файле `.env`
4. Убедитесь, что Spring Boot приложение запущено и доступно

## 📄 Лицензия

Этот проект является частью Event Booking Spring Boot Backend.
